<% form_tag :controller => 'openids', :action => 'create' do %>
   <p>
      <%= text_field_tag 'openid_url',nil, :class=>'openid_url'%> <%= submit_tag 'Login', :class => 'button' %>
   </p>
<% end %>
