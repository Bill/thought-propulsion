<% corkd_form_for( :twip, show, :url => url_for( :controller => 'twips', :action => 'show'), :html => { :method => 'get'} ) do |f| %>
<%= render :partial => f, :locals => {:readonly => true}%>
<% end %>