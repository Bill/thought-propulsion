<% airbudd_form_for( :twip, @twip, :controls => true, :scaffold => false, :url => url_for( :controller => 'twips', :action => 'update'), :html => { :method => 'put', :class => 'yui-skin-sam draft'} ) do |b| -%>
<%    @builder = b # ditto -%>
<%    inside_layout 'twips/_show_layout' do -%>
<%=      b.text_area :body, :label => nil, :id => 'rich-editor' %>
<%       b.buttons do |b| %>
<%=         b.button :save %>
<%=      b.button :cancel, :url => url_for( :controller => 'twips', :action => 'show', :id => b.object ) %>
<% end %>
<%    end -%>
<% end -%>