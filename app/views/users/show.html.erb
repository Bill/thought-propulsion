<% airbudd_form_for( :user, @user, :url => url_for( :controller => 'users', :action => 'show'), :html => { :method => :get} ) do |f| %>
<%= render :partial => f, :locals => {:readonly => true}%>
<% end %>